class Solution {
    public int knightDialer(int n) {
        
        // main body frame.
        for(int cell =0; cell<=9; cell++){
            r+ = (solve(n-1, cell))% M;
        }
    }
    solve(n, cell);{
        if(n==0)
            return 1;

        ans =0;
        for() //lopp for adjancey'
        {
            ans + = solev(n-1, cell)
        }

    }
}